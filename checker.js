module.exports = function Chk(x) {
    let found = false;
    for (i = 0; i < x.length; i++) {
        if (x[i] !== 'logs') {
            found = false
        } else {

            return found = true
        }
    }
    return found
}